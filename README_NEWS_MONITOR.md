# 新增：B站官方账号资讯监控器

原评论爬虫完整保留。新的轻量工具位于 [`news_monitor/`](news_monitor/README.md)，只使用 Python 标准库，不需要安装额外依赖。

Windows 用户进入 `news_monitor` 文件夹并双击 `run.bat` 即可。默认监控《重返未来：1999》官方账号，也可以在 `config.json` 中继续添加 UID。
